# Neutral engagement core

This is shared infrastructure, not a third communication channel. Accept an explicit channel scope;
string roots preserve dashboard compatibility. Never import either channel plugin. Keep lifecycle,
journal validation, atomic mutation, association and continuation rules here. Each channel gets
separate state and journals. Preserve historical dashboard ledger paths and one-time legacy pause
migration. Tests must prove independent removal and active-state voting.
